spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  datasource:
    username: teacher_assistant_backend
    password: q9TEAweN24aQ5uPvVN5
    url: jdbc:postgresql://localhost:5432/teacher_assistant
    driver-class-name: org.postgresql.Driver

application:
  curriculum:
    card-types: [lesson, individual_work, test, additional_lesson]  # add more
    card-type-to-color-mappings:
      lesson: red
      individual_work: blue
